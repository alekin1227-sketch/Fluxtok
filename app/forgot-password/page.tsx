import { Brand } from "@/components/brand";
export default async function ForgotPage({ searchParams }: { searchParams: Promise<Record<string, string | undefined>> }) { const q=await searchParams; return <main className="auth-shell auth-shell-brand"><section className="auth-card stack"><Brand/><div><h1 className="auth-title">Recuperar senha</h1><p className="muted">Informe seu e-mail de acesso.</p></div>{q.sent&&<div className="notice notice-success">Se o e-mail existir e o SMTP estiver configurado, as instruções serão enviadas.</div>}<form className="stack" action="/api/auth/forgot-password" method="post"><div className="field"><label>E-mail</label><input type="email" name="email" required/></div><button className="btn btn-primary">Enviar instruções</button></form><a className="text-link" href="/login">← Voltar ao login</a></section></main>; }
