import { Brand } from "@/components/brand";
export default async function ResetPage({ searchParams }: { searchParams: Promise<Record<string, string | undefined>> }) { const q=await searchParams;const token=q.token??"";return <main className="auth-shell auth-shell-brand"><section className="auth-card stack"><Brand/><div><h1 className="auth-title">Definir nova senha</h1><p className="muted">Use pelo menos 12 caracteres.</p></div>{q.error&&<div className="notice notice-error">Link inválido, expirado ou senha muito curta.</div>}<form className="stack" action="/api/auth/reset-password" method="post"><input type="hidden" name="token" value={token}/><div className="field"><label>Nova senha</label><input type="password" name="password" minLength={12} required/></div><button className="btn btn-primary">Alterar senha</button></form></section></main>; }
