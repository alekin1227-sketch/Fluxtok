import { Brand } from "@/components/brand";
export default async function LoginPage({ searchParams }: { searchParams: Promise<Record<string, string | undefined>> }) { const q=await searchParams; return <main className="auth-shell auth-shell-brand"><section className="auth-card stack"><Brand /><div><h1 className="auth-title">Bem-vindo ao Fluxtok</h1><p className="muted">Entre para acompanhar sua operação com creators.</p></div>{q.error&&<div className="notice notice-error">{q.error==="rate"?"Muitas tentativas. Aguarde alguns minutos.":"E-mail ou senha inválidos."}</div>}{q.reset==="ok"&&<div className="notice notice-success">Senha alterada. Entre novamente.</div>}<form className="stack" action="/api/auth/login" method="post"><div className="field"><label>E-mail</label><input name="email" type="email" autoComplete="email" required /></div><div className="field"><label>Senha</label><input name="password" type="password" autoComplete="current-password" required /></div><button className="btn btn-primary btn-lg">Entrar</button></form><div className="auth-split"><a className="text-link" href="/forgot-password">Esqueci minha senha</a><a className="text-link" href="/register">Criar conta grátis</a></div></section></main>; }
